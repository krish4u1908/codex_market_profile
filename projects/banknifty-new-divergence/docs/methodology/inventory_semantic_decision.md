# Semantic Decision Record

Frozen coordinate: `CAUSAL_BANKNIFTY_INDEX_REFERENCE_PRICE_BIN`.

Canonical name: `BN_REF_FUT_VOLUME_VPOC`; user label: **BN-REF FUT VOL-VPOC**.

Legacy diagnostic: `FUTURES_TRADE_PRICE_VPOC`; user label: **FUT-PRICE VOL-VPOC — Legacy diagnostic**. It is not interchangeable with the canonical control and must be disabled by default in a future RC2.

This deliberately supersedes R5 RC1 only for inventory coordinate semantics. It does not invalidate independently verified divergence detection.
