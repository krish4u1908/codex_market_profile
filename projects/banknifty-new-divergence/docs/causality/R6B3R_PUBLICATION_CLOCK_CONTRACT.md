# R6B3R publication clocks

Futures OI uses the REST receipt completing its elapsed window; Futures volume uses the latest included WebSocket receipt; CE/PE use exact REST receipts; breadth uses the maximum constituent receipt; joint participation uses the maximum required constituent effective time. A transition publishes at its first effective change. Episode and compatibility rows retain constituent clocks separately and never backdate evidence.
