# R6E1R-FINAL Open Issues

- Final focused all-nine v11 is independently verified PASS on pushed commit `85fd167...`. Earlier v3, v4, v6, the pre-parity full-six run, v9, and v10 remain rejected diagnostics and must not be cited as acceptance evidence.
- Six-session equivalence across all nine required schedules remains pending after rejected full-six v2 localized two repairable defects: the Intraday inventory path must use the frozen 5-second inventory join (basis remains exactly 2,000 ms), and incremental cross-layer publication must carry canonical chronological ordinals plus prior inventory state across canonical sessions while keeping fallback sessions local. Full performance evidence, final reports/package manifest, and isolated deployment also remain pending.
- The deployment package allowlist is deliberately stale after authorized source repairs and must be regenerated after final source freeze. Installation stays gated on six-session analytics. Host `Linger=no` requires administrator action for unattended reboot/logout startup; public reachability/firewall status remains to be tested after the gated install.
