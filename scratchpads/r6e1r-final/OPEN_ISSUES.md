# R6E1R-FINAL Open Issues

- Focused all-nine v8 passed for its exact bound engine, but a subsequent R6D visual-authority review found missing live dependency and participation-detail projection. The GUI/API allowlisted bytes are being repaired, so focused all-nine must be rerun against the newly sealed engine. Earlier v3, v4, v6, and the pre-parity full-six run remain rejected diagnostics and must not be cited as acceptance evidence.
- Six-session equivalence across all nine required schedules, full performance evidence, final reports/package manifest, and isolated deployment remain pending after the GUI parity repair.
- The deployment package allowlist is deliberately stale after authorized source repairs and must be regenerated after final source freeze. Installation stays gated on six-session analytics. Host `Linger=no` requires administrator action for unattended reboot/logout startup; public reachability/firewall status remains to be tested after the gated install.
