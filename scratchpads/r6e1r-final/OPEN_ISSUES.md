# R6E1R-FINAL Open Issues

- Focused nine-schedule v3 and v4 are rejected diagnostics. The source-hour-preserving sample, state/seal binding, and sparse-row schedule accounting are repaired; a fresh all-nine focused run remains required. Focused one-record v5 passes exactly.
- Six-session equivalence across all nine required schedules, full performance evidence, final reports/package manifest, and isolated deployment remain pending.
- The deployment package allowlist is deliberately stale after authorized source repairs and must be regenerated after final source freeze. Installation stays gated on six-session analytics. Host `Linger=no` requires administrator action for unattended reboot/logout startup; public reachability/firewall status remains to be tested after the gated install.
