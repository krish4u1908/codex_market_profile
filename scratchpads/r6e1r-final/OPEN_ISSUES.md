# R6E1R-FINAL Open Issues

- Push the reviewed current source/report commit on
  `fix/r6e1r-final-live-shadow`; record and test the exact pushed SHA. No
  verification tag exists.
- Independently verify on Hostinger the current engine closure (38/38,
  companion `715a82b4...`, aggregate `021935bc...`) and deployment closure
  (47/47, companion `c75d269d...`, aggregate `563e2d84...`) plus runtime config
  `5ce10587...`. Any mismatch blocks installation.
- Run fresh focused August 19 all-nine equivalence and full six-session all-nine
  equivalence through the real incremental checkpoint/callback path using fresh
  state/output roots. Earlier runs remain historical diagnostics, not current
  acceptance evidence.
- Require every canonical artifact and frozen reference to match with zero
  future joins, timestamp backdating, duplicate analytical IDs, prohibited
  runtime opens, and source mutations. Retain the August 17 rejection and all
  required restart/periodic-refresh evidence.
- Run the complete Hostinger regression suite, including sealed `/opt`
  comparison packages, Chromium/Playwright browser and geometry tests,
  ptrace/strace file-open audit, user-systemd verification, bubblewrap probes,
  health/readiness/API checks, and the largest-public-chart `<8 MiB` gate.
- The user-supplied August 20 archive is auxiliary only. Its read-only projection
  retained 68,171/68,171 identities with zero malformed records and zero source
  mutations, but it contains one receipt/path-session mismatch and cannot replace
  the canonical full-six collector input. The pre-fix large/original comparison
  failed append-only identity by 12 lifecycle and 24 cross-layer rows. Rerun it
  from fresh current-seal roots and require exact ledgers after the repair; do
  not promote this auxiliary rerun to canonical acceptance.
- If every analytical and host gate passes, install only the isolated backend on
  `127.0.0.1:18805` and one sanitized research gateway, protecting ports 8803 and
  8804 and all collectors. Prove backend SIGKILL recovery without manually
  restarting the gateway.
- Deployment status, selected external port, public URL, systemd live status,
  external browser evidence, port preservation, and collector preservation are
  all pending. Do not create `r6e1r-live-shadow-verified` before they pass.
- If Hostinger reports `Linger=no`, unavailable firewall authority, or another
  operational permission blocker, preserve and push verified analytical work
  and report the exact required administrator action without claiming deployment.
- Treat full-six v6 as rejected: its one-record harness omitted a visible
  checkpoint-lagging peer and produced 1,796 mirrored refusal rows. The bounded
  backlog repair passes 31/31 harness and 116/116 broader callback tests, but a
  fresh all-nine v7 from the exact published code/package commit remains mandatory.
- Measure cold-preload startup after installation and confirm it remains below
  the committed 8-GiB reclaim and 10-GiB hard memory boundaries.
