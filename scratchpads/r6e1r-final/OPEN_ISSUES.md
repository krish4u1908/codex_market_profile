# R6E1R-FINAL Open Issues

- Final focused all-nine v12 is independently verified PASS on pushed repair commit `71a868f...`. Earlier focused v3, v4, v6, v9 and v10, the pre-parity full-six runs, both focused-v12 invocation errors, and full-six v2-v9 remain rejected diagnostics and must not be cited as acceptance evidence.
- Six-session fresh A/B baseline matches every canonical artifact, ledger, frozen count, R6C2 row, and repaired R6D row. V7 was externally terminated by the six-hour interactive cgroup after five variant seals; it is rejected because final open/accounting evidence was still in memory, and its volatile partial roots are no longer present. V8 stopped when the non-lingering user manager exited and is also rejected. Fresh all-nine v10 is running from persistent roots under `r6e1r-v10-equivalence.service` and remains the only acceptance candidate. Full schedule/performance evidence and isolated deployment remain pending.
- The fail-closed 34-file deployment package is resealed and passes 122/122 static package/API tests. Installation stays gated on v10 plus complete regression. User lingering is now enabled (`Linger=yes`); public reachability/firewall status remains to be tested after the gated install.
- Cold-preload startup must be measured after installation to confirm it remains below the committed 8-GiB reclaim and 10-GiB hard memory boundaries. The prepared units now use integrity-aware post-start probes; only installation/runtime evidence remains pending.
