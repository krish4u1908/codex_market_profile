# R6E1R-FINAL Open Issues

- Final focused all-nine v12 is independently verified PASS on pushed repair commit `71a868f...`. Earlier v3, v4, v6, v9, v10, the pre-parity full-six runs, and both v12 invocation errors remain rejected diagnostics and must not be cited as acceptance evidence.
- Six-session fresh A/B baseline now matches every canonical artifact, ledger, and frozen count. A reference-only availability surface mismatch was repaired and verified against the preserved real material; a fresh all-nine six-session run remains required for acceptance. Full schedule/performance evidence, final reports/package manifest, and isolated deployment remain pending.
- The deployment package allowlist is deliberately stale after authorized source repairs and must be regenerated after final source freeze. Installation stays gated on six-session analytics. Host `Linger=no` requires administrator action for unattended reboot/logout startup; public reachability/firewall status remains to be tested after the gated install.
- Cold-preload startup must be measured after installation to confirm it remains below the committed 8-GiB reclaim and 10-GiB hard memory boundaries. The prepared units now use integrity-aware post-start probes; only installation/runtime evidence remains pending.
