# Local replay data (not published)

Place `expiry_oi_pressure_timeline.csv` and `normal_oi_pressure_timeline.csv` from the previously supplied v0.1.2 prototype package in this directory. These files are required by `server.py` at startup.

The source branch intentionally excludes user-supplied/reconstructed FYERS market-data exports and derived historical outcome tables. Preserve their provenance privately and do not commit them by accident.
