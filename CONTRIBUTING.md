# Contributing

Preserve causal clocks and frozen semantics. Changes to thresholds, synchronization, inventory coordinates, lifecycle states, or publication rules require a separately named research revision. Never rewrite the verified baseline or use outcomes to tune causal rules.

